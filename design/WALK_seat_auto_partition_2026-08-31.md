# WALK — seat-merged-group-auto-partition (2026-08-31)

Origin: chunk_set_id cycle walk W15 (operator paralyzed by the merged-
cards refusal, planning up to 12 trial commands the tool could run
itself). Operator ruled PURSUE 2026-08-31 (FOLLOWUPS commit 9348b113),
scheduled after the me-cli review closed green (it did, 0C/0I).

## Step 1 — the moment + three rulings (operator, verbatim)

Scenario re-grounded live: four strings, two 2-piece cards, both
stamped 12345; today's (improved) refusal still sends the operator
back to the plates. Auto-partition: 2 candidate pairings, the 4-byte
cross-chunk hash verifies the right one, rejects the wrong one (both
measured live this session).

- AP1 (success loudness — Q: seat + note + per-card R2 warnings?):
  operator, verbatim: "defect, attack, or idiosyncratic user choice at
  encode time". Read: YES to the note, and the note must NOT assert a
  single cause — a shared stamped id has three possible origins and
  the note names all three neutrally. (Corrects the walk author's
  draft, which asserted "mint defect" alone — the W15/M3 class again:
  never assert more than measured.)
- AP2 (both pairings verify — ambiguity): operator, verbatim:
  "agreed" — HARD REFUSAL, never a guess. Accident probability is
  ~2^-32 per wrong pairing; the reachable version is adversarial, and
  guessing would seat a wallet whose key pairing an attacker chose.
- AP3 (how far): operator, verbatim: "At least 3, cap at 5 if it takes
  too long". Read: support at least 3 colliding cards per id group;
  hard cap at 5 (above -> today's merged-cards refusal). The cap is
  performance-motivated in the ruling; the spec fixes it at 5
  regardless and requires timing to be measured at implementation.
  (Interpretation "cards, not candidate partitions" — flagged for R0
  and the operator to correct if misread.)

Walk CLOSED — compact because the operator answered the three edge
questions directly. Spec next; risk-set full gate applies.
