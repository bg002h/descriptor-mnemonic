# v0.15.1 Phase 5 Review — `md address --json` + insta snapshots

**Date:** 2026-05-03
**Reviewer:** feature-dev:code-reviewer (sonnet)
**Worktree:** `.worktrees/v0.15.1` branch `feat/v0.15.1`
**Commits:** 53520e8, 48bacbf, 16f3998
**Plan ref:** Phase 5 of `design/IMPLEMENTATION_PLAN_v0_15_1_address_and_network.md`

## Verdict: PASS — clean

No CRITICAL or IMPORTANT findings.

## Plan compliance

| Item | Status |
|---|---|
| `cmd/address.rs` collects `rows` first, then forks on `args.json` | PASS |
| JSON shape: `schema="md-cli/1"`, `network=args.network_str`, `addresses=[{chain,index,address}]` | PASS |
| CLI vocabulary network string (`"mainnet"` not `"bitcoin"`) | PASS |
| 3 snapshot tests in `cmd_address_json.rs`, gated `#![cfg(feature = "json")]` | PASS |
| 3 `.snap` files committed | PASS |
| Test count = 364 (361 + 3) | PASS |
| `cargo test` passes without `INSTA_UPDATE` override | PASS |

## LOW (defer to FOLLOWUPS at Phase 6 under `v0.15.2`)

### `v0.15.1-phase-5-low-1` — `network_str` lacks unused-warning suppressor when `json` feature off

`address.rs` has `let _ = args.json;` to silence the unused-warning when
the `json` feature is compiled out. `args.network_str` is referenced only
inside the `#[cfg(feature = "json")]` block; without `json` it's an
unread struct field. May or may not trigger a lint depending on
rustc/clippy version. Cosmetic. Confidence: 55.

## Conclusion

Phase 5 ships clean. Proceed to Phase 6 (docs + version bump + FOLLOWUPS append + ship).
