# v0.15 Phase 4 Review — Read/write subcommands (text mode)

**Date:** 2026-05-03
**Reviewer:** feature-dev:code-reviewer (sonnet)
**Worktree:** `.worktrees/v0.15-cli` branch `feat/v0.15-cli`
**Commits:** 731ef4b, b49ebfb, 49083f4, 53e8b7b, f13aba5, 1623821
**Plan ref:** `design/IMPLEMENTATION_PLAN_v0_15_cli.md` lines 2138-2685

## Verdict: PASS — no blockers

## CRITICAL / HIGH

None.

## MEDIUM

### M-1: `unimplemented!()` stubs cause panic, not clean error output

`Command::Vectors` / `Command::Compile` arms call `unimplemented!()` which panics with a Rust
thread-panic message (exit code 101) rather than returning `Err(CliError::BadArg(...))`. Other
unhandled paths use the formatted `eprintln!("md: {m}")` route.

**Status:** Will resolve in Phase 6 (`vectors`) and Phase 7 (`compile`) when those subcommands
ship. Until then, users invoking those commands see a panic trace. Not blocking.

## LOW

### L-1: No integration test for `--force-chunked`

The `--force-chunked` branch in encode is untested. Plan didn't require it.

### L-2: Plan test predicate `starts_with("md ")` already corrected to `"md1"`

Documented in commit `731ef4b`. No action needed.

## Plan compliance

All 5 tasks PASS. All required md_codec API calls verified. Both required tests per task present.
Stubs remain for vectors/compile per plan scope.
