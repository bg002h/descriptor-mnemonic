# SPEC v0.15.1 Review — r2 (verification pass)

**Date:** 2026-05-03
**Reviewer:** feature-dev:code-architect (sonnet)
**Worktree:** `.worktrees/v0.15.1` branch `feat/v0.15.1`
**SPEC reviewed:** `design/SPEC_v0_15_1_address_and_network.md` at commit `c937b9f`
**Predecessor:** `v0.15.1-spec-review-r1.md` (2 HIGH, 4 MEDIUM, 3 LOW)

## Verdict: PASS — clean

All 6 critical/important findings from r1 cleanly resolved. No new issues
introduced. L1/L2/L3 still appropriate to defer to FOLLOWUPS.md tier
`v0.15.2` at workflow step 8.

## Per-finding verdicts

| ID | Severity | Verdict | Reason |
|---|---|---|---|
| H1 | HIGH | PASS | `AddressArgs<'a>` construction note added; mirrors `EncodeArgs<'a>` dispatch verbatim |
| H2 | HIGH | PASS | `network_str: &'static str` on both `EncodeArgs` and `AddressArgs`; mainnet→`"mainnet"` vs `"bitcoin"` discrepancy explicitly documented; always-present rule stated |
| M1 | MEDIUM | PASS | Cargo.toml bullet notes workspace has no `version` field; crate-only bump |
| M2 | MEDIUM | PASS | Stale `parse/template.rs:706` citation replaced with `(defined in parse/template.rs; called from cmd/encode.rs:24)` |
| M3 | MEDIUM | PASS | BIP-published vectors retained for receive 0/1 + change/0; `--count 2` test added with distinct purpose (stdout line-count formatting); testnet derived in-test via `address_derivation.rs` recipe |
| M4 | MEDIUM | PASS | Two distinct bullet points with distinct error messages at distinct trigger layers; §Implementation surface corroborates |

## Cross-check questions resolved

- **`network_str` on `VerifyArgs`?** NOT needed. Verify is text-only output (reports OK/MISMATCH via exit code); no JSON serialization surface. Absence is correct.
- **Exit-codes paragraph readability after split?** Three-bullet layout reads cleanly; §Implementation surface corroborates both runtime message strings. No contradiction.
- **`--count 2` vs receive-0-and-1 duplication?** Distinct purpose: `--count 2` test asserts stdout-line-count formatting; not a re-pin of the same addresses. Explicit purpose statement included.

## New issues introduced

None.

## L1/L2/L3 deferral status

All three remain LOW severity; deferral to `design/FOLLOWUPS.md` tier
`v0.15.2` is still appropriate. No edits changed their severity. Will be
appended to FOLLOWUPS at workflow step 8 with `Source:` cite to
`v0.15.1-spec-review-r1.md`.

## Conclusion

SPEC is clean. Proceed to step 3 (invoke `superpowers:writing-plans` to
produce `design/IMPLEMENTATION_PLAN_v0_15_1_address_and_network.md`
anchored to this SPEC).
