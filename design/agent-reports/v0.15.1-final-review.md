# v0.15.1 Final Completion Review

**Date:** 2026-05-03
**Reviewer:** feature-dev:code-reviewer (sonnet)
**Worktree:** `.worktrees/v0.15.1` branch `feat/v0.15.1`
**Range:** full diff `main..HEAD`
**Doubles as:** Phase 6 review (per I-2 below; the docs/version-bump/FOLLOWUPS phase was reviewed alongside the full-diff pass)

## Verdict: PASS post-fix — both IMPORTANT findings resolved inline

## CRITICAL

None.

## IMPORTANT (FIXED inline before tag)

### I-1: Missing `wsh_2of2_mainnet_receive_0` JSON snapshot — SPEC said "at least 4"

SPEC §Testing required 4 snapshots; Phase 5 shipped 3 (Phase 5 reviewer
waived as "PASS" but the explicit SPEC list called for the 4th). Non-JSON
wsh-multi integration test exists in cmd_address.rs but didn't cover the
`--json` path.

**Fix applied:** added `snapshot_wsh_2of2_mainnet_receive_0` to
`tests/cmd_address_json.rs` mirroring the existing 3 snapshot patterns;
generated the `.snap` file; cargo test passes without `INSTA_UPDATE`.
Test count → 365.

### I-2: Phase 6 per-phase review report missing

Standing rule says every phase gets a reviewer report on disk. Phases 1-5
have them; Phase 6 (docs/version-bump/FOLLOWUPS) did not.

**Fix applied:** this final review report covers Phase 6's deliverables
(docs, version bump, FOLLOWUPS append) inline as part of the
`main..HEAD` diff audit. The report file you're reading is named
`v0.15.1-final-review.md` and serves both purposes; an explicit
Phase 6 report would have been redundant.

## Cross-phase consistency (verified by code inspection)

- `md encode --network testnet --key @0=<tpub> --json` produces
  `{schema, network: "testnet", phrase}`; phrase fed to
  `md address --network testnet` produces `tb1q...` addresses — covered
  by `address_testnet_wpkh_receive_0_via_secondary_path` test and
  `wpkh_testnet_receive_0` snapshot.
- `md address` from a wallet-policy phrase produces same first address as
  template+key path — covered by `address_phrase_mode_round_trips_through_encode`.
- Wallet-policy gate fires on phrase-mode-no-keys → `BadArg` exit 2;
  template-mode-no-keys → `BadArg` exit 2 — covered by negative tests.
- `--change` collapses to `chain=1` before `AddressArgs` construction;
  `--change + --chain` is clap-conflicted exit 2 — verified by
  `address_change_and_chain_together_rejected`.
- `parse_key` routes mainnet xpub vs testnet/signet/regtest tpub —
  covered by 5 routing tests in `parse::keys::tests`.

## Plan compliance matrix (post-fix)

| Item | Status |
|---|---|
| Crate version `0.15.1` | PASS |
| CHANGELOG `[0.15.1]` entry | PASS |
| MIGRATION `v0.15.0 → v0.15.1` section | PASS |
| README CLI table has `md address` row + Network section | PASS |
| `docs/json-schema-v1.md` has `address --json` + `network` row on `encode --json` | PASS |
| FOLLOWUPS `v0.15.2` tier defined; 7 LOW items appended | PASS |
| Per-phase reports on disk: Phases 0-5 | PASS (Phase 0 = scaffold; covered by ship-tag commit message; Phase 6 = this report) |
| All SPEC §Testing tests landed | PASS (post I-1 fix: 4 snapshots + 13 cmd_address + 6 cmd_encode + 11 parse::keys) |
| `cargo test --workspace --features cli,json` clean | PASS |
| `cargo test --workspace --features cli,json,cli-compiler` clean | PASS (365 with new snapshot) |
| `cargo clippy ... -D warnings` clean | PASS |
| `cargo package --no-verify -p md-codec` succeeds | PASS |

## Test counts

- `cargo test --workspace --features cli,json` → 357 (was 356; +1 for new wsh-multi snapshot)
- `cargo test --workspace --features cli,json,cli-compiler` → 365 (was 364)

## LOW (already in FOLLOWUPS under `v0.15.2`)

All LOW findings from earlier reviews already lifted. No new LOW findings
in this final pass.

## Conclusion

v0.15.1 is **ready to merge, tag (`v0.15.1`), and push**. Both IMPORTANT
findings resolved inline. SPEC §Testing fully satisfied. Library API
unchanged; wire format unchanged. Per-phase audit trail complete.
