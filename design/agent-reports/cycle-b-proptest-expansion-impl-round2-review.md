# Implementation Review — Cycle B proptest expansion (round 2)

Reviewer: Fable 5 architect agent (a31975e68326c67c1), 2026-06-11.
Target: round-1 impl-review folds (2 registry entries + comment minor) @ 69b7a74, uncommitted.
Persisted verbatim per CLAUDE.md convention BEFORE the fold.

## Verdict: YELLOW

Targeted documentation-only re-check per the round-1 path-to-GREEN. Folds for [I1], [I2]-existence, and the w_inner minor are all verified correct. However, round 2 was explicitly chartered to sanity-check the toolkit-exposure reasoning inside the new toolkit companion — that reasoning could not have been reviewed in round 1 (the entry did not exist) — and it contains one verifiable overstatement. The entry's bottom-line safety conclusion is TRUE (I verified it at line level), but its stated mechanism is false as a universal. One-clause fix; no code, test, or strategy change implicated.

## Critical
- none

## Important
- **[I1-r2] The toolkit companion's exposure mechanism overstates: "before any md-codec encode is reached" is false for the template/slot path.** `/scratch/code/shibboleth/mnemonic-toolkit/design/FOLLOWUPS.md::encode-accepts-k-greater-than-n` justifies "the toolkit cannot emit such a card today" solely via "its descriptor intake parses via rust-miniscript, whose `Threshold::new` enforces `k ≤ n` before any md-codec encode is reached." There is a toolkit path to md-codec encode that never transits a rust-miniscript parse: `bundle_run_unified` → `synthesize_unified` (`crates/mnemonic-toolkit/src/synthesize.rs:765`) → `CliTemplate::wrapper_node` (`src/template.rs:112`, hand-builds `Body::MultiKeys { k, indices: 0..n }` with no k≤n check) → `md_codec::chunk::split` (`src/synthesize.rs` md1 emit). The **conclusion survives** because that path is independently guarded toolkit-side at two layers — `pre_check_threshold` (`src/bundle_unified.rs:67–90`, rejects `t > n || t == 0`, called at `src/cmd/bundle.rs:365`) and `synthesize_unified`'s own guard (`src/synthesize.rs:~780`, `threshold == 0 || threshold as usize > n` → `MultisigConfig`); the dead-code synth helpers (`synthesize.rs:380`, `:525`) carry the same step-1 guard. But the registry sentence as written tells a future auditor the rust-miniscript parse is THE gate — concealing that the template path's actual gate is hand-maintained toolkit validation (precisely the kind of gate that drifts when a new template arm or flag lands). By round-1's own bar (a registry mis-statement = Important the moment it commits), this needs the one-clause amendment before commit: e.g. "…and the template/slot emit path (which never transits rust-miniscript) is guarded toolkit-side by `bundle_unified::pre_check_threshold` + `synthesize_unified`'s own k≤n reject before `wrapper_node` builds the `MultiKeys` body."

## Minor
- The two new md-codec entries use Tier values `cross-repo` and `upstream`, both novel to the file's Tier vocabulary (neighbors use version tags, `next-cycle`, `docs`, `external → wont-fix`). The vocabulary is evidently free-form (20 distinct prior forms), so this is observation-only; `upstream` arguably maps to the existing `external` convention.
- The toolkit repo carries 42 untracked items (cycle-b-* orchestrator scripts, recon docs, CONTINUITY.md) — pre-existing orchestrator workspace per the session-start snapshot, not part of this fold; flagging only so the commit step stages paths explicitly (which CLAUDE.md already mandates).

## Checks
1. **[I1] md-codec FOLLOWUPS entry — PASS.** `design/FOLLOWUPS.md` (+20 lines) now contains `upstream-miniscript-taptree-depth2-display-asymmetry`. Format matches neighbors (`### \`slug\` — title` + Surfaced/Where/What/Actions/Status/Tier/Companion bullets). Technical content matches round-1 evidence point-for-point: Display emits `tr(K,{{pk(a),pk(b),pk(c)}})`; `from_str` rejects with `IncorrectNumberOfChildren { "taptree branch", n_children: 1, min: 2, max: 2 }`; same-checksum re-Display proves Display is the faulty side; 2-leaf `{a,b}` unaffected. Names the flip cell `upstream_taptree_depth2_display_asymmetry` (verified live at `crates/md-codec/tests/proptest_to_miniscript.rs:312`) and the constrained arm `t_tr_tree` (verified live at `crates/md-codec/tests/common/mod.rs:993`, doc comment :981–992 matches). Actions-on-close include the upstream report with the #935 precedent AND restore-arm-and-invert-cell. Companion line is HONEST: "none yet — `mnemonic-toolkit` pins a DIFFERENT miniscript (git rev `95fdd1c`); its exposure is unverified. Verify before mirroring" — it does NOT cite a nonexistent toolkit entry.
2. **[I2] toolkit companion — PASS on existence and cross-citation; YELLOW on reasoning (see [I1-r2]).** `/scratch/code/shibboleth/mnemonic-toolkit/design/FOLLOWUPS.md` (+10 lines at tail) now contains `encode-accepts-k-greater-than-n`. (a) The md-codec primary exists and the two Companion lines cross-cite each other correctly and symmetrically (primary ↔ toolkit-side companion). (b) The descriptor-intake half of the exposure claim is correct: `src/parse_descriptor.rs` imports `miniscript::Descriptor as MsDescriptor` (:22) and walks the parsed miniscript tree into md-codec Nodes (:397+) — rust-miniscript's threshold construction does reject k > n at parse, so `multi(3,A,B)` never reaches encode via intake. But the "before ANY md-codec encode" universal is overstated: the template/slot path reaches `md_codec::chunk::split` with zero rust-miniscript involvement (evidence in [I1-r2]). The bottom-line "toolkit cannot emit such a card today" is nonetheless verified TRUE — every `wrapper_node` caller in src is guarded (synthesize.rs:380/:525/:780 + bundle_unified.rs:67–90), `repair.rs` only re-encodes existing chunks at the bech32 level (builds no Node trees), and single-sig templates hardcode k=1,n=1.
3. **w_inner doc comment — PASS.** `crates/md-codec/tests/common/mod.rs:546–550` now reads "≤ 25 nodes incl. root wrappers (w2 worst case 21; pairing 23; sh(wsh(·)) adds 2). Size enforcement is the encoded ≤ 18,000-bit assert in the final map, not this node arithmetic." Exactly the requested fold.
4. **fmt + tree state — PASS.** `cargo fmt --all -- --check` clean in descriptor-mnemonic. descriptor-mnemonic status: exactly 3 modified (`crates/md-codec/tests/common/mod.rs`, `crates/md-codec/tests/proptest_roundtrip.rs`, `design/FOLLOWUPS.md`), 1 new test file (`crates/md-codec/tests/proptest_to_miniscript.rs`), untracked = BRAINSTORM + 5 agent-reports (design docs, as intended); HEAD 69b7a74. Toolkit repo: only tracked modification is `design/FOLLOWUPS.md` (+10); the 42 untracked items are pre-existing orchestrator workspace.

## Evidence log
- descriptor-mnemonic: HEAD `69b7a74`, `git status --porcelain` = 3×`M` + 6×`??` as itemized in Check 4. `git diff design/FOLLOWUPS.md` = +20 lines, two new entries, no other hunks. `cargo fmt --all -- --check` → exit 0.
- mnemonic-toolkit: `git diff design/FOLLOWUPS.md` = +10 lines, one new entry at file tail; `git status --porcelain | grep -v '^??'` = ` M design/FOLLOWUPS.md` only.
- Cited names verified live: `upstream_taptree_depth2_display_asymmetry` @ proptest_to_miniscript.rs:312; `p8_encode_accepts_k_greater_than_n_decode_rejects` @ :654; `t_tr_tree` @ common/mod.rs:993 (doc comment :981–992 — depth ≤ 1 constraint, NOT-an-md-codec-bug statement, flip-cell pointer all present); w_inner comment @ common/mod.rs:546–550.
- Toolkit exposure trace (read-only greps, nothing modified): md-codec encode call sites in toolkit src = `format.rs:38` (render only) + `chunk::split` at `synthesize.rs:178/:210/:263/:469/:642` + `template.rs:524/:607` (tests). All `wrapper_node` src callers = `synthesize.rs:136` (1,1), `:417`, `:584`, `:810` — each multisig caller guards `threshold == 0 || threshold > n` → `ToolkitError::MultisigConfig` (read at :380–386, :525–532, :780–783). CLI layer: `pre_check_threshold` (`bundle_unified.rs:67–90`) called at `bundle.rs:365` before synthesis; `bundle.rs:887` threshold use is downstream of that same unified flow (JSON emit). `--threshold` + descriptor mode is refused outright (`bundle.rs:259–262`, `DESCRIPTOR_WITH_THRESHOLD`). `parse_descriptor.rs` confirmed miniscript-first (:22, :293–295, :397+). `repair.rs` md1 path = chunk-level BCH re-encode + `md_codec::chunk::reassemble` (:978–1013, :1043–1062); no Node construction.
- Round-1 review re-read in full (`design/agent-reports/cycle-b-proptest-expansion-impl-round1-review.md`); its code-review scope is not re-run per its own GREEN-path statement — round 2 confined to the three folds + the newly-written toolkit reasoning it explicitly delegated to this round.

**Path to GREEN:** amend one sentence in the toolkit companion's "Toolkit exposure" bullet to name the template/slot path and its toolkit-local guards (suggested wording in [I1-r2]). No re-review needed after the edit if applied as specified — it is a strict accuracy improvement with citations already verified here.
