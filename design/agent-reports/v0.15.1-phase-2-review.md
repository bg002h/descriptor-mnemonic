# v0.15.1 Phase 2 Review — `--network` on encode/verify + `encode --json` network field

**Date:** 2026-05-03
**Reviewer:** feature-dev:code-reviewer (sonnet)
**Worktree:** `.worktrees/v0.15.1` branch `feat/v0.15.1`
**Commits:** 770c174, 14d99af, ca37ed3, 490fed7
**Plan ref:** Phase 2 of `design/IMPLEMENTATION_PLAN_v0_15_1_address_and_network.md`

## Verdict: PASS — clean

No CRITICAL or IMPORTANT findings. Two LOW nits noted; both are pre-existing
or by-design.

## Plan compliance

| Item | Status |
|---|---|
| `CliNetwork` ValueEnum + `From<CliNetwork> for bitcoin::Network` | PASS |
| `CliNetwork::as_str()` kebab-cased names | PASS |
| `EncodeArgs.network` + `EncodeArgs.network_str` | PASS |
| `VerifyArgs.network` (no network_str — no JSON output) | PASS |
| `--network` default `CliNetwork::Mainnet` on Encode + Verify variants | PASS |
| JSON `"network"` field in encode --json output | PASS |
| End-to-end tpub round-trip via `encode --network testnet --key @0=<tpub>` | PASS |
| Test count = 348 (345 + 3) | PASS (verified by orchestrator) |
| Clippy clean | PASS (verified by orchestrator) |

## LOW (defer to FOLLOWUPS at Phase 6 under `v0.15.2`)

### `v0.15.1-phase-2-low-1` — `_ = args.force_long_code` silently ignored

`encode.rs` line 63 discards `force_long_code` unconditionally. Pre-existing
from v0.15.0 (not a Phase 2 regression). The flag is accepted by clap but
has no effect; documented as forward-compat in v0.15.0. Already tracked
implicitly in the v0.15.0 review reports. Confidence: 30.

### `v0.15.1-phase-2-low-2` — `parse_key` wildcard arm assumes `bitcoin::Network` is non-extensible

The `_ =>` arm in `parse_key` groups Testnet/Signet/Regtest correctly per
BIP 32. Non-exhaustive risk if a future `bitcoin` crate version adds a new
`Network` variant. Idiomatic for an external crate's non-exhaustive enum;
plan intentionally chose this pattern. Confidence: 25.

## Conclusion

Phase 2 ships clean. Proceed to Phase 3 (`cmd/address.rs` skeleton + Address
variant + dual-input mode).
