# v0.15.1 Phase 1 Review — `parse_key` network plumbing

**Date:** 2026-05-03
**Reviewer:** feature-dev:code-reviewer (sonnet)
**Worktree:** `.worktrees/v0.15.1` branch `feat/v0.15.1`
**Commits:** 49e31d1, 488b35f, 6b60b2b, 84a1aea
**Plan ref:** Phase 1 of `design/IMPLEMENTATION_PLAN_v0_15_1_address_and_network.md`

## Verdict: PASS — clean

No CRITICAL or IMPORTANT findings. One LOW nit deferred to v0.15.2.

## Plan compliance

| Item | Status |
|---|---|
| `TESTNET_XPUB_VERSION = [0x04, 0x35, 0x87, 0xCF]` BIP 32 standard | PASS |
| `parse_key(arg, ctx, network: bitcoin::Network)` signature | PASS |
| All 6 prior test calls re-pinned to `Network::Bitcoin` | PASS |
| 5 new routing tests (testnet/signet/regtest accept; testnet rejects xpub; mainnet rejects tpub) | PASS |
| Error messages satisfy `contains("expected testnet")` / `contains("expected mainnet")` | PASS |
| `ABANDON_TPUB_DEPTH3_BIP84` valid testnet xpub at depth 3 | PASS |
| encode.rs line 22 and verify.rs line 23 thread `Network::Bitcoin` | PASS |
| Throwaway derive test deleted before commit | PASS |
| Test count = 345 (340 + 5) | PASS |
| Clippy clean | PASS |

## LOW (defer to FOLLOWUPS at Phase 6 under `v0.15.2`)

### `v0.15.1-phase-1-low-1` — `#[allow(dead_code)]` on `ABANDON_TPUB_DEPTH4_BIP48` is redundant inside `#[cfg(test)]`

The const carries `#[allow(dead_code)]` but it lives inside a `#[cfg(test)]`
block; outside test builds, the compiler doesn't see it at all. Cosmetic.
Confidence: 40.

**Source:** this report.

## Conclusion

Phase 1 ships clean. Proceed to Phase 2 (`--network` on encode/verify +
`encode --json` network field).
