# v0.15 Phase 0 Review — Cargo manifest, scaffold, CliError

**Date:** 2026-05-03
**Reviewer:** feature-dev:code-reviewer (sonnet)
**Worktree:** `.worktrees/v0.15-cli` branch `feat/v0.15-cli`
**Commits:** a3f99a7, 7b3bd21, fad9ca8, a759c00, ed11b40
**Plan ref:** `design/IMPLEMENTATION_PLAN_v0_15_cli.md` lines 123–535

## Verdict: PASS — no blockers for Phase 1

## CRITICAL

None.

## HIGH

None.

## MEDIUM

### M-1: Phase 1 Task 1.1 code shipped inside Phase 0 commits

**Confidence: 90**

The Phase 0 plan's `main.rs` scaffold (Task 0.4, lines 372–498) does not include `mod parse;`. That
declaration, `parse/mod.rs`, and `parse/keys.rs` are explicitly Phase 1 Task 1.1 scope (plan lines
543–548). All three are present in the Phase 0 ship:

- `crates/md-codec/src/bin/md/main.rs` line 2: `mod parse;`
- `crates/md-codec/src/bin/md/parse/mod.rs`
- `crates/md-codec/src/bin/md/parse/keys.rs`

The content of `parse/keys.rs` matches the Phase 1 plan verbatim; the code is correct. This is a
phase-boundary slip, not a correctness defect.

**Orchestrator note (post-review):** The reviewer surveyed the working tree, which by then included
Phase 1 Task 1.1 (committed *after* the Phase 0 ship-tag commit `ed11b40`). The Phase 0 ship-tag
commit itself (`--allow-empty`) does not contain those files; they are in `ce893c9`
(`feat(v0.15/phase-1): ...`). No corrective action needed.

## LOW

### L-1: Ship-tag test count comment is stale

**Confidence: 85**

Task 0.5 Step 1 predicts "Total ok: 256 (253 baseline + 3 scaffold)". With the 3 unit tests inside
`error.rs` (which the plan introduces but doesn't credit in the count), the actual total at the
ship tag is 259. Tests pass; the comment is a counting error in the plan itself.

## Plan compliance matrix

| Task | Deliverable | Result |
|------|-------------|--------|
| 0.1 | `[workspace.dependencies]` miniscript 13.0.0 pin | EXACT MATCH |
| 0.2 | `md-codec/Cargo.toml` — version 0.15.0, features, bin entry, dev-deps | EXACT MATCH |
| 0.3 | `error.rs` — 7 variants, Display, Error, From&lt;md_codec::Error&gt;, 3 unit tests | EXACT MATCH |
| 0.4 | `main.rs` clap scaffold — all 7 subcommands, flags, dispatch arms | EXACT MATCH |
| 0.4 | `tests/scaffold.rs` — 3 assert_cmd tests | EXACT MATCH |
| 0.5 | Ship-tag commit message | EXACT MATCH |
