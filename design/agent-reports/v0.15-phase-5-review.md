# v0.15 Phase 5 Review — JSON shadow types + --json wiring

**Date:** 2026-05-03
**Reviewer:** feature-dev:code-reviewer (sonnet)
**Worktree:** `.worktrees/v0.15-cli` branch `feat/v0.15-cli`
**Commits:** 270eb72, 2fbe8d5, a093c01, 8c0acda, 5d4f4fb, cac4851
**Plan ref:** `design/IMPLEMENTATION_PLAN_v0_15_cli.md` lines 2690-3367

## Verdict: PASS — no blockers

## CRITICAL / HIGH

None.

## MEDIUM

### M-1: JSON `schema` key not guaranteed first (alphabetical ordering)

`serde_json::Map` without `preserve_order` sorts keys alphabetically. `"schema"` is always present
but may not appear first. Tests use `contains()` so they pass. Matches plan; if downstream tooling
needs schema-first, add `preserve_order` feature to serde_json.

### M-2: `JsonTlv` Option fields serialize as `null` (no `skip_serializing_if`)

Matches plan exactly. Verbose but correct.

## LOW

- `--json` silently no-ops when built without `json` feature (default features include it).
- Chunked JSON encode path has no integration test (plan didn't require one).
- `#[derive(Default)]` on `JsonTlv` is unused (matches plan).

## Plan compliance

All 16 plan items verified. Library remains serde-free (json feature gated, types don't derive
Serialize). All 9 plan-specified tests present and passing.
