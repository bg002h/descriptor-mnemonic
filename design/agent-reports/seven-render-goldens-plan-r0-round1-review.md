# R0 Review — md-codec seven-fragment render goldens (PLAN) — Round 1
Reviewer: Fable 5, 2026-06-12. Verified against descriptor-mnemonic origin/main e798431 (working tree clean, `main...origin/main`, only the plan-doc untracked). Toolkit origin/master d3fafaf confirmed. Scratch test built, run, and deleted; tree clean after.

## Verdict: RED (0 Critical / 2 Important)

## Critical
None.

The crux question — are all 7 shapes valid TYPED wsh descriptors that `p6_chain` accepts — is **independently re-confirmed, not just recon-trusted**. I rebuilt all seven typed `Node` trees via `common::{descriptor_with_pubkeys, wrap, node2, keyarg, timelock}` and ran them through render → reparse-fixed-point → mainnet address on the live tree (miniscript 13.0.0 pin): 7/7 pass, and **all seven rendered strings (including checksums `#ywq2et2d #4hddhe69 #xtal5qj5 #qlzgpwq4 #qkdzs9qu #mjt9mr4g #9f6svydl`) and all seven candidate golden addresses match the recon's table byte-for-byte**. Type-rule spot-checks all hold: `or_b(pk,s:pk)` = Bdu×Wdu ✓; `or_c(pk,v:pk)` = Bdu×V → V, B-ified by `and_v(·,True)` → renders `t:or_c(` ✓; `d:v:older(144)` = DupIf over Vz → Bd, hosted under `or_i` ✓; `j:pk` — pk is Bdu**n** (sig never empty) ✓; `n:and_v(v:pk,older)` — n-property propagates from v:pk ✓; `or_i(pk,False)` → `u:pk(` ✓; `and_v(v:pk,True)` → `tv:pk(` ✓. No shape belongs in P7. The sugar contract (plan §2/§6) is verified correct: rust-miniscript Displays `u:`/`t:`/fused `dv:`/`tv:`, never a literal `0`/`1` — the plan's `contains()` markers all appear in the live rendered strings.

(B) grammar-extension safety, **as specified** ("one production per fragment — the seven proven shapes"): safe. `t_segwit_tree`'s output is consumed only under `wrap(Tag::Wsh, …)` (`common/mod.rs:1017`) so the extension is wsh-only as claimed; the T strategy feeds only the P6 property (`proptest_to_miniscript.rs:481`) and the anti-vacuity test (`:849`) — no other property can break. Lock-pool parameterization of the two lock-bearing shapes is type-safe for both lock classes (older/after are both Bz; v:LOCK is Vz). All seven shapes carry ≥1 key (grammar invariant, `common/mod.rs:821`) and use ≤2 key slots (16-cap fine). The hardcoded count exists only in the const's type annotation `[Tag; 23]` (`:765`) — the anti-vacuity loop iterates the array (`:856`), so 23→30 is mechanical and breaks nothing else.

## Important

**I1 — (C) is mis-targeted: wrong line for `v06-corpus-or-c-coverage`, and the same-class `v06-corpus-j-n-wrapper-coverage` entry is missed.** Live `design/FOLLOWUPS.md` @ e798431: `:1041` is the **Where:** line of `v07-phase2-or-c-unwrapped-test-docstring-drift` (a v0.7.1 docstring-hygiene record), NOT `v06-corpus-or-c-coverage` — that entry lives at `:925-:933` (Status `:932` cites the deleted `or_c_unwrapped_tap_leaf_byte_form` / `t_or_c_tap_leaf_round_trips` hand-AST tests). Worse, the directly-adjacent `v06-corpus-j-n-wrapper-coverage` (`:934-:942`, Status `:941` cites the deleted `j_wrapper_tap_leaf_byte_form` / `n_wrapper_tap_leaf_byte_form`) is **the same vacated-resolution class and covers j:/n: — two of this cycle's seven fragments** — and the plan omits it. Executed as written, (C) would re-ground one wrong entry and leave one right one dangling. Fix: (C)'s entry set = the THREE v06-corpus entries (`v06-corpus-d-wrapper-coverage` :916-:923 ✓ as cited, `v06-corpus-or-c-coverage` :925-:933, `v06-corpus-j-n-wrapper-coverage` :934-:942); drop the :1041 cite (touching the v0.7-era process entries is optional — note the scope choice, since ~7 entries total cite the dead file: :904, :1000, :1031-:1035, :1041, :1074-:1078, :1093-:1096, :1135-:1139, but those are historical process records, not fragment-coverage claims).

**I2 — (B) must pin the production typing boundary; naive pool-composition is demonstrably type-invalid and would panic P6.** I empirically confirmed the hazard the plan leaves implicit: `or_b(older(144), s:pk)` and `j:older(144)` both REFUSE type-check (`to_miniscript_descriptor` → Err "the `j:` wrapper around a fragment which might be satisfied by an input of size zero") — and P6 step 1 **panics** on Err (`proptest_to_miniscript.rs:52-54`, no prop_filter). The existing grammar's house style is pool-composition (`leaf_any`/`bdu0`/`lock` pools threaded everywhere), so "extend `t_segwit_tree` with one production per fragment" invites an implementer to parameterize the new arms over those pools — which would produce nondeterministic P6 reds (lock/hash children lack `d`/`n` where `or_b`/`or_c`/`j:`/`n:` require them). Fix: one sentence in §2(B): *new productions are the FIXED seven proven shapes — children only from `t_ka()` (keys) and `t_lock_node` in the two proven lock positions (`dv:LOCK`, `n:and_v(v:pk,LOCK)` — safe for both lock classes); do NOT route `leaf_any`/`bdu*`/`hash` pools into `or_b`/`or_c`/`j:`/`n:` child positions (counterexamples: `or_b(older,s:pk)`, `j:older` both type-fail).* With that pinned, (A)+(B) is the right call — (B) is what dissolves the gap class, and I verified it composes safely under that constraint.

## Minor

**M1 — Anchor the `contains()` markers.** Bare `u:` / `j:` / `tv:` are currently safe (xpubs are base58, checksums bech32 — no stray colons) but the anchored forms are free and self-documenting, and all are verified present in the live renders: `or_b(`, `t:or_c(`, `dv:older(144)`, `j:pk(`, `n:and_v(`, `u:pk(`, `tv:pk(`.

**M2 — (D) is not a pure "de-ignore."** Live toolkit `parse_descriptor.rs:2420-2424` @ d3fafaf: `arm_dup_if` has the cited `#[ignore]` string verbatim but an **empty stub body** ("Walker arm exists for completeness; counted as 1 stub") — de-ignoring alone yields a vacuous pass. The work is writing the body (`wsh(or_i(pk(@0/<0;1>/*),dv:older(144)))` + `find_tag(Tag::DupIf)`, mirroring `arm_non_zero` at :2426). The ignore reason IS disproven (my scratch reparsed `dv:older(144)` via `Descriptor::from_str` on the same 13.0.0). Default-split is fine per single-repo cycle discipline, but if split, **file the toolkit FOLLOWUP in this cycle** so the disproven-ignore doesn't go tracking-less (plan currently files nothing).

**M3 — Plan §1 "two resolved FOLLOWUP entries still cite that dead file" is an undercount** (~7 entries do); subsumed by I1's fix — state the scope choice.

**R0 answers to §5:** Q1: (A)+(B), with I2's constraint. Q2: split (D), with M2's FOLLOWUP condition. Q3: confirmed — `self_test_wsh_and_v_pk_older_144` (:136-151) is exactly the derive-once/prefix-verify/pin pattern, and reparse-fixed-point (:75-82) is the independent oracle; the recon's candidate addresses are now double-derived (recon run + this review) so bring-up should match them exactly. Q4: agree, no wire/decode cells needed — W anti-vacuity (`:829-831`) already enforces wire generation of all 7; no testnet variants needed (house style is mainnet-only goldens; step-4 address differential covers the rest).

## Citation audit
- `to_miniscript.rs` arms: DupIf :337 ✓, NonZero :341 ✓, ZeroNotEqual :345 ✓, OrB :368 ✓, OrC :374 ✓, False :458 ✓, True :459 ✓; all funnel to `Miniscript::from_ast` :478 ✓; **no `#[cfg(test)]`** (grep 0 hits, 538 lines ✓).
- `proptest_to_miniscript.rs`: `W_TARGET_TAGS: [Tag; 34]` :727 ✓ contains all 7 (True :745, False :746, DupIf :751, NonZero :752, ZeroNotEqual :753, OrB :757, OrC :758) ✓; `T_TARGET_TAGS: [Tag; 23]` :765 ✓ omits all 7 (plus Check/SortedMulti/SortedMultiA/RawPkH — 23+7=30=34−4, internally consistent) ✓; anti-vacuity loops :829/:856 ✓; P6-over-T-only :481 ✓.
- SHA claims: origin/main = e798431 ✓; 422b049..e798431 = exactly one commit touching only `design/FOLLOWUPS.md` (+11/−1) ✓ "docs-only" accurate. The **:1041 mis-attribution is the plan's own addition** (recon §5 said only "~:920-929").
- `hand_ast_coverage.rs`: directory `src/bytecode/` gone; deleted at `5350f8a` "release: md-codec v0.12.0 — strip v0.x, flatten v11" ✓ exactly as plan §1 states.
- Toolkit: origin/master = d3fafaf ✓; `arm_dup_if` at `parse_descriptor.rs:2421` (plan's ca7d7bc:2420, 1-line benign drift) with the cited ignore string verbatim ✓ — but see M2 (stub body).
- FOLLOWUPS cites: ~:922 ✓; **~:1041 ✗ wrong entry** (see I1).
- NO-BUMP: ✓ correct — test files + `design/FOLLOWUPS.md` only; no src, no wire, no clap surface; PATCH escalation path correctly reserved for a found render bug (none found — 7/7 re-verified green).

**Gate status: RED — fold I1 + I2, then re-dispatch for Round 2.** Both folds are small text amendments; no scope change. The empirical core (shapes, goldens, sugar markers, NO-BUMP, (A)+(B) judgment) is sound and now double-verified.
