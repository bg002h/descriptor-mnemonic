## R0 Adversarial Review — LANE2-walker SPEC (`Terminal::RawPkH` backport to md-cli)

**VERDICT: GREEN — 0 Critical / 0 Important. Cleared to implement.** 5 Minor citation-hygiene defects (line-number/fn-attribution drift); none alters any edit, the test design, or the gate outcome.

### Method
Verified every load-bearing claim against the LIVE `descriptor-mnemonic` checkout at HEAD `f18a0275d0dd` (= origin/main = the spec's stated SHA — confirmed). The CRITICAL fold and the YAGNI-disproof premise were **independently reproduced** in a throwaway worktree at `f18a027` (applied the exact spec edits, ran the full CI gate battery, then removed the worktree).

### Load-bearing claims — ALL VERIFIED

**1. YAGNI premise DISPROVEN (the thing under test).** Reproduced: `MsDescriptor::<DescriptorPublicKey>::from_str("wsh(c:expr_raw_pkh(00..00))")` PARSES (Display `wsh(expr_raw_pkh(..))#2ey4n0ax`) and its AST is `Wsh → Check → RawPkH` (probe printed `TOP=Check`, `INNER=RawPkH`). Today `walk_root` errors `unsupported miniscript fragment: expr_raw_pk_h(..)` — the gap is real. Bare `wsh(expr_raw_pkh(..))` is rejected as `non-T miniscript` (confirms the footgun warning). The `Check` arm (template.rs:939-962) `if let PkK/PkH` guards do NOT match a RawPkH child, so it falls to `Tag::Check{Children:[walk(RawPkH)]}` → catch-all today. The spec's two-level descent assertion is correct.

**2. CRITICAL fold reproduced (Edit 1b is genuinely MANDATORY).** miniscript 13.0.0 `Terminal` has exactly **28** variants and is **NOT** `#[non_exhaustive]` (verified at `miniscript-13.0.0/src/miniscript/decode.rs:90`; the line above `pub enum Terminal` is a `///` doc-comment, no attribute). The md-cli walker has **27** top-level arms (8-space-indent `Terminal::` minus the two `if let` inside `Check`); RawPkH is the sole gap. Reproduced both directions: arm-only under `RUSTFLAGS="-D warnings" cargo build -p md-cli` FAILS with `error: unreachable pattern … and 24 other patterns collectively make this unreachable … -D unreachable-patterns implied by -D warnings … help: add #[allow(unreachable_patterns)]` (verbatim match to the spec); arm + `#[allow]` → build, `cargo clippy --workspace --all-targets -- -D warnings`, full `cargo test -p md-cli` (all bins GREEN, 0 failures), `cargo doc` (RUSTDOCFLAGS=-D warnings), `cargo fmt --check`, and `cargo test --workspace` ALL clean.

**3. The RawPkH arm is the EXACT toolkit mirror, STRICTLY RawPkH only.** Byte-identical to toolkit `parse_descriptor.rs:739-742`: `Terminal::RawPkH(h) => Ok(Node { tag: Tag::RawPkH, body: Body::Hash160Body(h.to_byte_array()) })`. `bitcoin::hashes::Hash` already imported at template.rs:907 (in-fn); `Tag`/`Body`/`Node` types in scope via module `use` at 767/768; no new import. Scope-fence verified: there is no `Terminal::SortedMultiA` variant in 13.0.0 (sortedmulti handled at `WshInner::SortedMulti`/`walk_sh`), so the toolkit slug's \"24 arms + sortedmulti_a\" framing is correctly identified as over-scoped.

**4. md-codec needs NO bump — verified present.** `tag.rs:84` variant, `tag.rs:133` `Tag::RawPkH => (0x21, None)` encode, `tag.rs:197` `0x21 => Ok(Tag::RawPkH)` decode, `tree.rs:318` `Tag::Ripemd160 | Tag::RawPkH` 160-bit body decode, `to_miniscript.rs:614` intentional REFUSE (\"not constructible through miniscript's public API\") — all present and untouched. Decode-side refuse is correctly fenced into the Change-4 Scope note (no full md1→descriptor round-trip claimed).

**5. The round-trip test is real.** Ran the spec's exact `walk_rawpkh_wsh_check_emits_rawpkh_node` verbatim → GREEN, asserting `Wsh.body.Children[0].tag==Check`, `Check.body.Children[0].tag==RawPkH`, `Body::Hash160Body([0;20])`. `Body::Children(Vec<Node>)` and `Body::Hash160Body([u8;20])` shapes confirmed against `md-codec/src/tree.rs:20/68`. bin name is `md` (Cargo.toml:19) → `--bin md` correct. TDD RED→GREEN ordering note (RED test compiles only pre-arm) is accurate.

**6. CI gates, version sites, crate-labels, pins.** `ci.yml:10` `-D warnings`, `ci.yml:47` clippy, `ci.yml:59` fmt, `ci.yml:65` RUSTDOCFLAGS — all at cited lines. `md-cli/Cargo.toml:3` and `Cargo.lock:480` both `0.9.2`. Fold-2 crate-label fix correct: `text.rs:182` render arm and `json.rs:207` `Tag::RawPkH => JsonTag::RawPkH` are in **md-cli** `format/`, not md-codec. Toolkit pins md-cli at frozen tag `descriptor-mnemonic-md-cli-v0.7.1` (`cross-tool-differential.yml:50`, `manual.yml:86`) → no CI re-fire from v0.10.0, no new clap flag → no manual-mirror cascade. Both FOLLOWUPS entries confirmed to lack a Companion line (Change 6a/6b correctly add them); the md \"Why deferred\" \"rejects expr_raw_pkh upstream of the walker\" rationale is empirically FALSE (the `c:`-form parses fine) — the spec's correction is warranted.

### Minor defects (fold for hygiene; non-blocking)
Five line-number / function-attribution drifts — see findings. The most notable: recon-correction #1 attributes the placeholderless-rejection to `resolve_placeholders:411`, but it originates in `lex_placeholders:151` (line 411 raises a different string). The behavioral claim is correct and the test path is unaffected (`walk_root`, not `parse_template`), so this is purely a citation fix. The remaining four are ±1-2 line drifts on slug/quote-anchored FOLLOWUPS/CHANGELOG edits that apply cleanly regardless.

### Bottom line
The spec is implementation-ready. The single highest-risk item — the CRITICAL claim that the arm makes the catch-all an unreachable-pattern HARD COMPILE ERROR under `-D warnings`, mandating the paired `#[allow(unreachable_patterns)]` — is empirically true and reproduced. md-codec stays `0.39.0`; the change is strictly the RawPkH arm + the mandatory allow + the walk-side test + docs/version/CHANGELOG/FOLLOWUPS. Fold the 5 Minor citation fixes inline (no re-review gate required) and proceed.