# v0.15.1 Phase 4 Review — `--chain`/`--change`/`--index`/`--count` semantics + golden vectors

**Date:** 2026-05-03
**Reviewer:** feature-dev:code-reviewer (sonnet)
**Worktree:** `.worktrees/v0.15.1` branch `feat/v0.15.1`
**Commits:** da00da4, 7e262e5, 5437748
**Plan ref:** Phase 4 of `design/IMPLEMENTATION_PLAN_v0_15_1_address_and_network.md`

## Verdict: PASS — clean

No CRITICAL or IMPORTANT findings.

## Plan compliance

| Item | Status |
|---|---|
| Loop `index..(index+count)` with `checked_add` overflow guard | PASS |
| 8 new tests in tests/cmd_address.rs (mainnet 0/1, change/0, testnet 0, --count 1000, 1001 reject, --chain 5, --change+--chain reject, wsh-multi 2-of-2) | PASS |
| Test count = 361 (353 + 8) | PASS |
| Clippy clean | PASS |
| wsh-multi test uses `bitcoin::PublicKey::new(cpk.0)` + Builder chain | PASS |

## LOW (defer to FOLLOWUPS at Phase 6 under `v0.15.2`)

### `v0.15.1-phase-4-low-1` — `account_xpub_testnet` wrapper specified in plan but inlined

The plan called for an `account_xpub_testnet(path)` convenience wrapper;
the actual test inlines `account_xpub("m/84'/1'/0'", Network::Testnet)`
which is functionally equivalent and arguably cleaner. Confidence: 60.

## Conclusion

Phase 4 ships clean. Proceed to Phase 5 (`--json` output + insta snapshots).
