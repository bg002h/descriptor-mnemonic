# v0.15 Phase 9 Review — Docs and release

**Date:** 2026-05-03
**Reviewer:** feature-dev:code-reviewer (sonnet)
**Worktree:** `.worktrees/v0.15-cli` branch `feat/v0.15-cli`
**Commits reviewed:** 98be1b9, 018a19b, 15115dc, ff77a9c, 86d9e19
**Plan ref:** `design/IMPLEMENTATION_PLAN_v0_15_cli.md` lines 4241-4546

## Verdict: PASS post-fix — three findings fixed in `cf0a651`

## CRITICAL / HIGH

### B-1: README had four stale v0.7-era sections (FIXED)

The new `## CLI` section landed correctly at the top, but four obsolete blocks remained:
- `## Cargo features` table listed `compiler` and `test-helpers` (don't exist), missed `json`
- A second `## CLI` section with wrong subcommand names (`from-policy`, `--policy`)
- `## Test vectors` section referencing the removed `gen_vectors` binary and `v0.1.json` corpus
- `## Status` block claiming `v0.7.1` and `673+ tests`

**Fix:** all four sections removed. Replaced features table with v0.15-accurate three-row table.

### B-2: `encode --json` schema falsely claimed `phrase` always present (FIXED)

`docs/json-schema-v1.md` said `phrase` is "always: yes". Actual behavior: `phrase` is present
*only* when `--force-chunked` is absent; with `--force-chunked` the output emits `chunk_set_id`
plus a previously-undocumented `chunks` array.

**Fix:** schema table now correctly marks `phrase` as `iff *not* --force-chunked` and adds the
missing `chunks` row.

## MEDIUM

### M-1: `--json` claim was overbroad (FIXED)

CHANGELOG and README originally said "every read/write subcommand" but `verify` has no `--json`.
**Fix:** docs now scope the claim to encode/decode/inspect/bytecode/compile and explain that
`verify` uses exit code.

## LOW (deferred)

- `--force-long-code` flag is silently ignored in encode (kept for forward-compat per plan task 4.1; no harm).
- `--path` flag declared on encode but discarded (plumbing wired in `parse/path.rs` with explicit comment; out-of-scope per plan task 4.1).

## Clippy allows assessment

All `#[allow(missing_docs)]` and `#[allow(dead_code)]` annotations from commit `86d9e19` reviewed
and judged appropriate — none mask real bugs.

## Plan compliance (post-fix)

| Task | Status |
|------|--------|
| 9.1 README CLI section + cleanup | PASS post-fix |
| 9.2 docs/json-schema-v1.md correct | PASS post-fix |
| 9.3 MIGRATION v0.14→v0.15 | PASS |
| 9.4 CHANGELOG [0.15.0] | PASS post-fix |
| 9.5 cargo test ≥ 340 | PASS (340 with-compiler, 332 without) |
| 9.5 cargo clippy -D warnings | PASS |
| 9.5 cargo package | PASS (122 files, 496 KiB) |
