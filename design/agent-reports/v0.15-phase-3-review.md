# v0.15 Phase 3 Review — Text formatter

**Date:** 2026-05-03
**Reviewer:** feature-dev:code-reviewer (sonnet)
**Worktree:** `.worktrees/v0.15-cli` branch `feat/v0.15-cli`
**Commits:** 6e36057, d1c70f5, a14377b
**Plan ref:** `design/IMPLEMENTATION_PLAN_v0_15_cli.md` lines 1833-2133

## Verdict: PASS — no blockers

## CRITICAL / HIGH

None.

## MEDIUM

### M-1: `fmt_md1_id` and `fmt_template_id` over-allocate (cosmetic)

`String::with_capacity(64)` for a 16-byte input renders 32 hex chars; capacity is 2× needed.
`fmt_policy_id` correctly uses `with_capacity(32)`. Functionally fine, just inconsistent.

### M-2: Stale version qualifier in comment

`fmt_policy_id_fingerprint` doc reads "v0.14's `WalletPolicyId` has no `fingerprint()` method";
the version qualifier will become misleading after v0.15 ships. The choice itself (slicing) is
permanent.

## LOW

### L-1: `use` items appear after the `tests` module

Legal Rust, no functional impact; matches the plan's "Append" instruction literally.

### L-2: No round-trip test exercises `use_site_path_overrides`

`render_key`'s override-lookup branch is correct but uncovered by tests. Plan didn't require it.

## Plan compliance

All 8 plan items verified: `descriptor_to_template`, tap-tree `{,}` rendering, override-aware
`render_key`, 4 round-trip tests, 5 identity helpers, correct API against `identity.rs`/`chunk.rs`,
`mod format;` in main.rs, correct `format/mod.rs`. Non-blocking — proceed.
